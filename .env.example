# Frontend environment template.
# Copy to `.env` for local dev. For production (Cloudflare Pages / Lovable),
# set VITE_API_BASE_URL as a BUILD environment variable instead.
#
# Must point at the deployed backend. Update the domain to match your backend.
VITE_API_BASE_URL=https://cpbackend.onrender.com/api
